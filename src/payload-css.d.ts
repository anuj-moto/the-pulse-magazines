/**
 * Type stub for Payload's side-effect CSS import.
 * `@payloadcms/next/css` resolves to a stylesheet and ships no type
 * declarations — declaring it here keeps `next build`'s type-check happy.
 */
declare module '@payloadcms/next/css'
